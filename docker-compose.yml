services:
  hub:
    build: .
    container_name: innovation-services-hub
    ports:
      - "8080:8080"
    environment:
      - BASE_PATH=/hub
    networks:
      - apex-internal
    volumes:
      - ./services.db:/app/services.db
    restart: always

networks:
  apex-internal:
    external: true
